//const imgCont = document.querySelector('.imgcont');

//imgCont.addEventListener('dragstart', function() {
   // imgCont.classList.add('animate'); // Triggers the animation
//});

//imgCont.addEventListener('dragend', function() {
    //imgCont.classList.remove('animate'); // Removes the animation when drag ends
//});
